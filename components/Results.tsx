import React, { useEffect } from 'react';
import { AuditResult } from '../types';
import { AdSpace } from './AdSpace';

interface ResultsProps {
  result: AuditResult;
  onReset: () => void;
}

export const Results: React.FC<ResultsProps> = ({ result, onReset }) => {
  
  useEffect(() => {
    window.scrollTo(0, 0);
  }, []);

  return (
    <div className="animate-fade-in max-w-4xl mx-auto px-4 py-12">
      
      {/* Header */}
      <div className="mb-8 flex justify-between items-end">
        <div>
           <h2 className="text-3xl font-light text-gray-900">Audit Report</h2>
           <p className="text-gray-500 mt-2">Generated by Bhai Auditor AI</p>
        </div>
        <button 
          onClick={onReset}
          className="hidden sm:block text-sm text-gray-500 hover:text-black font-medium border border-gray-200 px-4 py-2 rounded-lg hover:border-gray-400 transition-all"
        >
          New Audit
        </button>
      </div>

      <div className="bg-white rounded-2xl border border-gray-100 shadow-xl shadow-gray-100/50 overflow-hidden">
        
        {/* Score Header */}
        <div className="p-10 border-b border-gray-100 flex flex-col items-center text-center bg-gray-50/30">
          <div className="mb-4">
             <div className="flex items-center space-x-2">
               {[1, 2, 3, 4, 5].map((star) => (
                 <svg 
                    key={star}
                    className={`w-10 h-10 ${star <= Math.round(result.rating) ? 'text-yellow-400 fill-current' : 'text-gray-200 fill-current'}`} 
                    viewBox="0 0 24 24"
                  >
                    <path d="M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z"/>
                 </svg>
               ))}
             </div>
          </div>
          <span className="text-5xl font-bold text-gray-900 tracking-tight">{result.rating}<span className="text-2xl text-gray-400 font-light">/5</span></span>
          <p className="text-sm text-gray-400 font-medium uppercase tracking-widest mt-2">Overall Quality Score</p>
        </div>

        <div className="p-8 sm:p-12 space-y-10">
          
          {/* Summary */}
          <section>
            <h3 className="text-sm font-bold text-gray-900 uppercase tracking-widest mb-4 flex items-center">
              Overview
            </h3>
            <p className="text-xl text-gray-600 leading-relaxed font-light border-l-4 border-black pl-6 py-1">
              {result.summary}
            </p>
          </section>

          <AdSpace />

          <div className="grid grid-cols-1 md:grid-cols-2 gap-12">
            {/* Issues */}
            <section>
               <h3 className="text-sm font-bold text-gray-900 uppercase tracking-widest mb-6 flex items-center text-red-600">
                UI/UX Issues
              </h3>
              <ul className="space-y-4">
                {result.issues.map((issue, idx) => (
                  <li key={idx} className="flex items-start text-gray-700">
                    <span className="flex-shrink-0 w-6 h-6 flex items-center justify-center rounded-full bg-red-50 text-red-500 text-xs font-bold mr-3 mt-0.5">
                      {idx + 1}
                    </span>
                    <span className="leading-relaxed text-sm font-medium">{issue}</span>
                  </li>
                ))}
              </ul>
            </section>

             {/* Technical Analysis */}
             <section>
               <h3 className="text-sm font-bold text-gray-900 uppercase tracking-widest mb-6 flex items-center text-blue-600">
                Technical Code Faults
              </h3>
              <ul className="space-y-4">
                {result.technical_analysis.map((tech, idx) => (
                  <li key={idx} className="flex items-start text-gray-700">
                    <svg className="w-5 h-5 text-blue-500 mr-3 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />
                    </svg>
                    <span className="leading-relaxed text-sm font-mono text-gray-600">{tech}</span>
                  </li>
                ))}
              </ul>
            </section>
          </div>

          {/* Advice */}
          <section className="bg-green-50 rounded-2xl p-8 border border-green-100">
             <h3 className="text-sm font-bold text-green-800 uppercase tracking-widest mb-3">
              Premium Advice
            </h3>
            <p className="text-green-900 font-medium text-lg italic">
              "{result.advice}"
            </p>
          </section>

        </div>
      </div>
      
      <div className="mt-8 text-center sm:hidden">
         <button 
          onClick={onReset}
          className="text-gray-900 font-medium underline"
        >
          Check another website
        </button>
      </div>
      
      <AdSpace />
    </div>
  );
};